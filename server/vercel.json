{
  "version": 2,
  "name": "thechatapp-server",
  "builds": [{ "src": "./server.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/(.*)", "dest": "/" },
    { "src": "/routes/(.*)", "dest": "/" },
    { "src": "/utils/(.*)", "dest": "/" }
  ]
}
